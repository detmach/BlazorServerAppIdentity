@page "/Account/ConfirmEmailChange"

@if (_isLoading == true)
{
    <p><em>Loading...</em></p>
}
else
{
    <IdentityUI.Components.Alert AlertMessage="_alertMessage" />

    @if (_isEmailConfirmed)
    {
        <IdentityUI.Components.LoginForm />
    }
}
